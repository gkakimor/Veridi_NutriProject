/**
 * Roteiro mínimo para ordens de E2E — PRODUCTION-ROUTE-ASSIGNMENT-01.
 *
 * Mora em `fixtures/producao.mjs` desde E2E-BASELINE-REDESIGN-WAVE-01-02, onde
 * foi provado contra a API real (`roteiro-aplicado-planeja-ordem.mjs`). Este
 * arquivo mantém o import das suítes que ainda o usam.
 */
export { aplicarRoteiroNaOrdem } from "../fixtures/producao.mjs";
