import type { HelpHint, HelpTopic } from "../help-content";

/** Compras e estoque — ordens de compra, recebimentos, posição, lotes, movimentações, materiais de clientes e inventário. */
export const suprimentosTopics = {} satisfies Record<string, HelpTopic>;

export const suprimentosHints = {} satisfies Record<string, HelpHint>;
