import type { HelpHint, HelpTopic } from "../help-content";

/** Cadastros, gestão, qualidade e administração. */
export const cadastrosTopics = {} satisfies Record<string, HelpTopic>;

export const cadastrosHints = {} satisfies Record<string, HelpHint>;
