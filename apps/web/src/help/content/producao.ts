import type { HelpHint, HelpTopic } from "../help-content";

/** Produção — ordens, picking, formulações, templates e produto acabado. */
export const producaoTopics = {} satisfies Record<string, HelpTopic>;

export const producaoHints = {} satisfies Record<string, HelpHint>;
