import type { HelpHint, HelpTopic } from "../help-content";

/** Comercial — projetos, amostras, pedidos, expedições e faturamento. */
export const comercialTopics = {} satisfies Record<string, HelpTopic>;

export const comercialHints = {} satisfies Record<string, HelpHint>;
